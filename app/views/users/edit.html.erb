<% content_for :sidebar do %>
  <% if current_user.admin? %>
    <p><%= link_to "Delete #{@user.name}", @user, :method => :delete, :confirm => "Are you sure?" %></p>
  <% end %>
<% end %>

<div id="second_component">
  <%= render :partial => "users/user_form" %>
  <h4> update your avatar </h4>
  <%= render partial: "avatars/form"%>
</div>

 <div id="mid_component">
  <% @paintings.each do |p|%>
    <div class="row-fluid">
      <div class="span12">
        <p>
      <%= p.title %>
      <div>
        <%= image_tag p.image_url(:thumb) %><%= p.description %>
      </div>
      <div>
          <% if current_user.admin? %>
            <%= link_to "Delete", p, data: { method: :delete, confirm: "Are you sure you want to delete this painting?" } %>
          <% else %>
            <%= nil %>
          <% end %>
        </div>
        </p> 
      </div>
    </div>
  <% end %>
</div>
